{
  "name": "@wix/{%projectName%}",
  "version": "1.0.0",
  "description": "",
  "main": "serverless.js",
  "scripts": {
    "build": "rm -rf ./build && npx tsc",
    "test": "npm run build && serverless-cli test .",
    "dev": "npx tsc -w",
    "deploy": "npm run build && git add . && git commit -m",
    "postdeploy": "git push && serverless-cli deploy @wix/{%projectName%}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/http-errors": "^1.6.3",
    "@types/jest": "^25.1.1",
    "@types/node": "^13.7.0",
    "@types/uuid": "^7.0.2",
    "@wix/serverless-testkit": "^1.0.402",
    "@wix/serverless-api": "latest",
    "axios": "^0.19.2",
    "chai": "^4.2.0",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.0",
    "typescript": "^3.9.5",
    "uuid": "^7.0.3"
  },
  "dependencies": {
    "@wix/serverless-api": "^1.0.416"
  }
}
